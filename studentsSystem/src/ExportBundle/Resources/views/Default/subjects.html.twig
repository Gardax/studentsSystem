<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
</head>
<body>
<table border="1">
    <thead>
        <tr>
            <td>#</td>
            <td>Име на дисциплина</td>
            <td>Хорариум(Л)</td>
            <td>Хорариум(У)</td>
        </tr>
    </thead>
    <tbody>
    {% for subject in subjects %}
        <tr>
            <td>{{ subject.id }}</td>
            <td>{{ subject.name }}</td>
            <td>{{ subject.workloadLectures }}</td>
            <td>{{ subject.workloadExercises }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>